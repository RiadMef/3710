import { PlanteFilterPipe } from './plante-filter.pipe';

describe('PlanteFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanteFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
